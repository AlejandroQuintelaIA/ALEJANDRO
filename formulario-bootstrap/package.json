{
  "name": "formulario-bootstrap",
  "version": "1.0.0",
  "description": "Formulario de contacto con Bootstrap y pruebas automatizadas",
  "main": "src/index.html",
  "scripts": {
    "start": "live-server src",
    "deploy": "gh-pages -d src",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "author": "Tu Nombre",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^5.1.3",
    "gh-pages": "^3.2.3"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "@playwright/test": "^1.40.0"
  }
}